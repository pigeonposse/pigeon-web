{
	"name": "@pigeonposse/docker",
	"version": "0.0.4",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "dovenv transform",
		"dev-down": "docker compose down",
		"dev-up": "docker compose up -d",
		"image-down": "docker compose -f docker-compose.image.yml down -d",
		"image-up": "docker compose -f docker-compose.image.yml up -d"
	}
}
